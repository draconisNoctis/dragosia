import { AdvantagesToStringPipe } from './advantages-to-string.pipe';

describe('AdvantagesToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new AdvantagesToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
